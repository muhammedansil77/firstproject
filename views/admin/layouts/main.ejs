<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'LuxTime Admin' %></title>

  <!-- BOOTSTRAP -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- FONT -->
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <!-- PAGE-SPECIFIC CSS -->
    <!-- PAGE-SPECIFIC CSS -->
  <% if (typeof cssFile !== 'undefined' && cssFile) { %>
    <link rel="stylesheet" href="<%= cssFile %>">
  <% } %>

  <style>
    body {
      font-family: 'Manrope', sans-serif !important;
      background: #0f0b1a !important;
      color: #e2e8f0 !important;
    }
    .btn-primary {
      background: linear-gradient(135deg, #7c3aed, #ec4899) !important;
      border: none !important;
    }
    .btn-primary:hover {
      background: linear-gradient(135deg, #6d28d9, #db2777) !important;
    }
    .table-dark {
      --bs-table-bg: #1a1a2e !important;
      --bs-table-hover-bg: #16213e !important;
    }
    .modal-content {
      background: #1a1a2e !important;
      color: white !important;
      border: 1px solid #333 !important;
    }
    .form-control {
      background: #16213e !important;
      border: 2px solid #333 !important;
      color: white !important;
    }
    .form-control:focus {
      border-color: #8b5cf6 !important;
      box-shadow: 0 0 0 0.25rem rgba(139, 92, 246, 0.3) !important;
    }
  </style>

  <!-- CropperJS CSS (global) -->
  <link href="https://unpkg.com/cropperjs@1.5.13/dist/cropper.min.css" rel="stylesheet"/>

</head>
<body>

  <!-- HEADER -->
  <%- include('../partials/header') %>

  <div class="d-flex">
    <!-- SIDEBAR -->
    <%- include('../partials/admin-sidebar') %>

    <main class="flex-grow-1 p-4">
      <%- body %>
    </main>
  </div>

  <!-- AXIOS -->
   <!-- AXIOS -->
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js" defer></script>

  <!-- REQUIRED FOR TOASTR -->
  <script src="https://code.jquery.com/jquery-3.7.0.min.js" defer></script>

  <!-- TOASTR -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js" defer></script>

  <script>
    // keep toastr init but run after load
    window.addEventListener('DOMContentLoaded', function () {
      toastr.options = {
        closeButton: true,
        progressBar: true,
        positionClass: "toast-top-right",
        timeOut: 4000
      };
    });
  </script>

  <!-- CropperJS JS (global) - ensure this loads BEFORE page JS -->
  <script src="https://unpkg.com/cropperjs@1.5.13/dist/cropper.min.js" defer></script>

  <!-- PAGE-SPECIFIC JS (deferred) -->
  <% if (typeof pageJS !== 'undefined' && pageJS) { %>
      <script src="/admin/js/<%= pageJS %>" defer></script>
  <% } %>
</body>
</html>