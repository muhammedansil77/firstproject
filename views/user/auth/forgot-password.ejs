<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Forgot Password - LuxTime</title>

  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

 <link rel="stylesheet" href="/user/css/forgotpassword.css">
</head>
<body>

  <!-- Header -->
  <header class="site-header">
    <div class="container-lg px-4 py-3 d-flex align-items-center gap-3">
      <div class="brand-badge">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" xmlns="http://www.w3.org/2000/svg" style="width:18px;height:18px;">
          <circle cx="12" cy="12" r="10" stroke="currentColor"></circle>
          <path d="M12 6V12L16 14" stroke="currentColor" stroke-linecap="round"></path>
        </svg>
      </div>
      <h1 class="h4 mb-0 display-serif fw-bold" style="color: #fff;">LuxTime</h1>
    </div>
  </header>

  <!-- Main -->
  <main class="d-flex align-items-center justify-content-center py-5" style="min-height: calc(100vh - 86px);">
    <div class="container px-4">
      <div class="row justify-content-center">
        <div class="col-12 col-md-10 col-lg-8 max-w-lg">

          <div class="text-center mb-4">
            <h2 class="display-serif fw-bold" style="font-size: clamp(2rem, 4vw, 3.5rem);">Forgot Password?</h2>
            <p class="muted-lux mb-0">Enter your email and we'll send you a link to reset your password</p>
          </div>

          <div class="glass-card">

            <!-- Success/Error messages (EJS) -->
            <% if (typeof message !== 'undefined' && message) { %>
              <div class="alert alert-success bg-success bg-opacity-10 border-success border-opacity-25 text-success d-flex align-items-center" role="alert">
                <div><%= message %></div>
              </div>
            <% } %>

            <% if (typeof error !== 'undefined' && error) { %>
              <div class="alert alert-danger bg-danger bg-opacity-10 border-danger border-opacity-25 text-danger d-flex align-items-center" role="alert">
                <div><%= error %></div>
              </div>
            <% } %>

            <!-- Form -->
            <form action="/user/forgot-password" method="POST" class="row g-3">
              <div class="col-12">
                <label class="form-label text-white fw-medium small text-uppercase" style="letter-spacing:.12em;">Email Address</label>
                <input type="email" name="email" required placeholder="your@email.com"
                       class="form-control" />
              </div>

              <div class="col-12 mt-3">
                <button type="submit" class="btn btn-luxury w-100">Send Reset Link</button>
              </div>
            </form>

            <!-- Back link -->
            <div class="text-center mt-4">
              <a href="/user/login" class="muted-lux small text-decoration-none">← Back to Sign In</a>
            </div>

          </div> <!-- glass-card -->

        </div>
      </div>
    </div>
  </main>

  <!-- Bootstrap JS bundle (optional for any JS components) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
