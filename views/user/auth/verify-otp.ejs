<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Verify OTP - WatchCo</title>
  
    <link rel="stylesheet" href="/user/css/otp.css">
</head>
<body>
  <!-- Toast Container -->
  <div class="toast-container" id="toastContainer"></div>

  <!-- Header -->
  <header>
    <div class="container">
      <div class="header-content">
        <div class="logo-circle">
          <svg class="logo-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="9" stroke-width="2"/>
            <path d="M12 6v6l4 2" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </div>
        <div class="brand-name">WatchCo</div>
      </div>
    </div>
  </header>

  <!-- Main Content -->
  <main>
    <div class="card-container">
      <div class="card">
        <!-- Title -->
        <div class="title-section text-center">
          <h1>Enter your code</h1>
          <p class="subtitle">
            We sent a 6-digit code to <strong id="emailDisplay">example@email.com</strong>
          </p>
        </div>

        <!-- OTP Form -->
        <form id="otpForm">
          <div class="otp-inputs">
            <input type="text" maxlength="1" class="code-input" inputmode="numeric" pattern="[0-9]" required />
            <input type="text" maxlength="1" class="code-input" inputmode="numeric" pattern="[0-9]" required />
            <input type="text" maxlength="1" class="code-input" inputmode="numeric" pattern="[0-9]" required />
            <input type="text" maxlength="1" class="code-input" inputmode="numeric" pattern="[0-9]" required />
            <input type="text" maxlength="1" class="code-input" inputmode="numeric" pattern="[0-9]" required />
            <input type="text" maxlength="1" class="code-input" inputmode="numeric" pattern="[0-9]" required />
          </div>

          <!-- Timer -->
          <div class="timer-section">
            <p class="timer-text">
              Code expires in <span id="timer" class="timer">01:00</span>
            </p>
          </div>

          <!-- Submit Button -->
          <button type="submit" class="submit-btn" id="verifyBtn">
            <span class="btn-text">Verify Account</span>
          </button>
        </form>

        <!-- Resend Button -->
        <div class="resend-section">
          <button type="button" id="resendBtn" class="resend-btn">
            <span id="resendText">Resend code in 60s</span>
          </button>
        </div>
      </div>
    </div>
  </main>

  <script src="/user/js/otp.js"></script>
</body>
</html>